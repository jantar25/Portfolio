// function updateList() {
//     const title = [...document.querySelectorAll('h1','h2')].sort((a,b)=>{
//         return Math.abs(a.getBoundingClientRect().top)-Math.abs(b.getBoundingClientRect().top)
//     })

//     document.querySelectorAll('.circle').forEach(c=>c.classList.remove('circle'));
//     document.querySelectorAll('.dot')[[...document.querySelectorAll('h1','h2')].indexOf(title[0])].classList.add('circle')
// }
// updateList();
// window.addEventListener('scroll',()=>{
//     updateList() ;
// })

// console.log(...document.querySelectorAll)